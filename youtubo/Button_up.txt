ttk.Button은 기본적으로 font나 width/height가 바로 잘 안 먹습니다.
👉 Style을 만들어서 적용해야 제대로 크게 만들 수 있어요.

아래처럼 수정하세요.

✅ 버튼 크게 + 폰트 크게 만드는 방법
import tkinter as tk
from tkinter import ttk

# 스타일 생성
style = ttk.Style()
style.configure(
    "Big.TButton",
    font=("맑은 고딕", 16, "bold"),   # 폰트 크게
    padding=10                       # 버튼 내부 여백 (버튼 크기 커짐)
)

ttk.Button(
    self.root,
    text="⬇ 다운로드",
    command=self.start_download,
    style="Big.TButton"
).pack(pady=20)

🔥 더 크게 만들고 싶다면
style.configure(
    "Big.TButton",
    font=("맑은 고딕", 20, "bold"),
    padding=(20, 15)   # (좌우, 상하)
)

📌 버튼 가로폭도 키우고 싶다면
ttk.Button(
    self.root,
    text="⬇ 다운로드",
    command=self.start_download,
    style="Big.TButton",
    width=20   # 글자 기준 가로 크기
).pack(pady=20)

💡 참고

padding = 버튼 자체 크기 증가

font = 글자 크기 증가

width = 가로 길이 증가 (글자 수 기준)